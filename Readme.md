# OONI PM Tools

This repository includes code which is useful to project management.

The labels inside of this repository also consistute the base template labels
common to all OONI repositories.

To sync the labels with the ones in this repo run: `python sync-labels.py`.

Other useful scripts in here include `add-to-teams.py`, which adds issues to
the relevant github project board in the icebox column.
